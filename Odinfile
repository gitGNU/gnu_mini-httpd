# Build the http daemon.

httpd == %httpd.cc.sm +lib=(libscheduler/libscheduler.a) \
                      +define='DOCUMENT_ROOT="/home/simons/projects/httpd"' \
                      :exe

%httpd.cc.sm == <<
    main.cc
    httpd.cc

%clean ! == !<<
    rm -f httpd libscheduler.a
